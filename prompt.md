Now you will start on a new project I'm working on, it's directory is in /3D-bar,it is used for website deployment game, so you need to compress and optimized all NEWLY ADDED object, texture picture or 3D files. Here are the requirements and hints from the previous AI Agent. Please read the directory and learn what you'll do in the future, reply to me ALWAYS in Chinese. Thanks. Here are the notes:

请继续基于当前 `Blender` 工作区开发，不要回滚现有修改，也不要覆盖根目录源资产。项目是 Three.js/Vite 第一人称酒吧-电视 portal 主题解谜游戏，生产入口在 `3D-bar`；普通 `/` 进入酒吧，`/hr2` 可直接出生在第二场景恐怖房。当前已完成并验证：保险箱密码 `117`、wet/CIDER 谜题、门禁密码 `42425142`、酒吧到恐怖房的一次性切换、恐怖房眼睛墙纸与天花板、中央电视堆、电视静态画面及屏幕溢光、过门后门板自动关闭。最新酒吧模型为 `public/models/cozy_bar_v008-e4ad253c.glb`，恐怖房模型为 `public/models/horror_room_v001-62e06a81.glb`；最近完整验证为 46 项测试通过、`SITE VALIDATION PASSED`、Vite build 成功。下一阶段目标：把恐怖房墙上的电视逐个设计成可解锁 Portal，玩家完成每台电视对应条件后可点击穿越到不同主题场景。请先阅读 `src/main.js`、`src/game-state.js`、`src/click-interactions.js`、`src/horror-room.js`、`src/scene-lifecycle.js`、`src/tv-prop.js` 及相邻测试，遵循现有纯状态转换、中心准星射线交互、一次只持有一个物品、场景预载和递归资源释放模式；先做创意/需求澄清并形成主题、解锁顺序、返回规则、持久化范围和失败反馈的明确设计，再动代码。建议将 Portal 配置做成稳定 ID 驱动的数据表，状态逻辑放入独立纯模块，交互 eligibility 放在 `click-interactions.js`，传送与场景清理继续交给 lifecycle；不要把关卡规则堆进 `main.js`，也不要复制电视材质或逐帧创建纹理/灯光。每台 Portal 应有明确的锁定、可解锁、预载中、可进入、错误和已完成视觉状态；点击前要经过距离、遮挡和状态检查，加载失败必须可重试，快速重复点击必须幂等。明确决定 Portal 是单向还是可返回；若可返回，必须保留目标场景状态并避免重复下载，若单向，则进入后及时释放恐怖房几何、材质、纹理、灯光和定时器。常见坑包括：Blender +Y 映射 Three.js -Z、父子矩阵导致对象漂移或缩放爆炸、透明交互代理遮挡后方目标、屏幕 emissive 不会真实照亮墙面、GLB 缓存哈希更新遗漏、旧 `dist` 或旧端口造成假回归、克隆电视共享材质后误 dispose、Portal 屏幕与玻璃层 z-fighting、预载竞态、玩家在目标场景出生点嵌入碰撞体，以及 `/hr2` 路径不应预载酒吧模型。严格按 TDD：先给纯 Portal 状态写 RED 测试，再写交互 eligibility、预载幂等/失败重试、切换与资源释放、返回策略、目标出生点碰撞测试；实现后先跑聚焦测试，再跑 `npm test && npm run build && git diff --check`。新增 Blender 场景时使用内容哈希文件名，写 clean-import 验证脚本检查对象名、Portal 锚点世界坐标、朝向、碰撞边界、面数、纹理尺寸与包体积，并同步更新 `src/main.js`、`scripts/validate-site.mjs`、README。最终必须在全新端口做 Playwright 实机验证：普通 `/`、快捷 `/hr2`、桌面与移动端各检查一次；逐台确认锁定反馈、解锁动画、点击跳转、加载失败重试、重复点击不会重复切换、目标场景出生正确、返回/单向规则正确、旧场景确实释放；同时截屏并做 canvas 非黑屏像素检查。不要提交或部署，除非用户明确要求。
